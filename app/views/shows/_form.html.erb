<%= form_with(model: [@federation, show], local: true) do |form| %>
  <% if show.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(show.errors.count, "error") %> prohibited this show from being saved:</h2>

      <ul>
        <% show.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :day_of_week %>
    <%= form.text_field :day_of_week %>
  </div>

  <div class="field">
    <%= form.label :number_of_matches %>
    <%= form.text_field :number_of_matches %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
